export default async function AuthAdmin(req, res) {
    // let { NEXT_PUBLIC_API } = process.env
    // let headers =await req.headers
    // let cookie = JSON.stringify(req) 
    // console.log(headers);
    // console.log(await Object.keys  (headers));
    // console.log(JSON.stringify(req.headers))
}
// export const config = {
//     matcher: '/admin/*',
//   };